TELEGRAM_BOT_TOKEN = Your Telegram bot token
KOREAEXIM_AUTHKEY = (Optional) Exchange rate API key
ALERTS_TABLE_NAME = DynamoDB table name (auto-created)
USERS_TABLE_NAME = DynamoDB table for user accounts
EVENTBRIDGE_BUS = EventBridge bus name
SECRET_KEY = JWT signing key
ACCESS_TOKEN_EXPIRE_MINUTES = Access token TTL (default 30)
AWS_REGION = Region for AWS service calls (defaults to `us-east-1`)