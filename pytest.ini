[pytest]
pythonpath = src
# Only run tests from the tests/ directory
testpaths = tests
# Filter out deprecation warnings from external libraries (botocore)
filterwarnings =
    ignore::DeprecationWarning:botocore.*
